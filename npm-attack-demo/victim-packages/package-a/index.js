/**
 * Normal utility functions
 * This is a legitimate package with no malicious code
 */

function add(a, b) {
  return a + b;
}

function multiply(a, b) {
  return a * b;
}

module.exports = {
  add,
  multiply
};
